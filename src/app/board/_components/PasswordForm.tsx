'use client'
import React from 'react'
import styled from 'styled-components'

const StyledForm = styled.form``

const PasswordForm = () => {
  return <StyledForm>1111</StyledForm>
}

export default React.memo(PasswordForm)
